/**
 * Usage Dashboard - read-only owner view for gateway traffic and harness efficiency.
 * Wing: owner | Topic: usage-dashboard | Updated: 2026-09-11
 */

import type { ServerResponse } from "node:http";

function page(): string {
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>SlncTrZ Usage</title>
<style>
:root{color-scheme:light dark;--bg:#eef0f3;--surface:#fbfcfd;--surface2:#f4f6f8;--text:#1a1d21;--muted:#697586;--line:#dfe3e8;--accent:#2f5a9e;--accent2:#7c3aed;--good:#18794e;--warn:#b54708}
@font-face{font-family:"SlncHertine";src:url(/assets/fonts/SlncHertine.woff2) format("woff2");font-display:swap;font-weight:400;font-style:normal}
*{box-sizing:border-box}body{margin:0;min-height:100dvh;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;line-height:1.5;-webkit-font-smoothing:antialiased}
a{color:inherit}.shell{max-width:76rem;margin:auto;padding:2rem 1rem 4rem}.topbar{display:flex;align-items:center;justify-content:space-between;gap:1rem;margin-bottom:1.1rem}.brand{font-family:"SlncHertine",system-ui,sans-serif;font-size:2rem;font-weight:600;letter-spacing:.02em}.nav{display:flex;gap:.5rem;align-items:center}.nav a,.range button{display:inline-flex;align-items:center;justify-content:center;text-decoration:none;border:1px solid var(--line);background:var(--surface);color:var(--text);border-radius:9px;padding:.5rem .75rem;font-weight:600;font-size:.82rem;cursor:pointer}.nav a:hover,.range button:hover{border-color:#aab2bd}.hero{display:flex;justify-content:space-between;gap:1rem;align-items:end;margin:0 0 1rem}.hero h1{font-size:1.65rem;line-height:1.2;margin:.1rem 0 .35rem}.hero p{margin:0;color:var(--muted);max-width:48rem;font-size:.9rem}.range{display:flex;gap:.35rem;flex-wrap:wrap}.range button.active{background:var(--accent);border-color:var(--accent);color:#fff}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:1rem}.card{background:var(--surface);border:1px solid var(--line);border-radius:14px;box-shadow:0 1px 2px rgba(16,24,40,.04);padding:1.1rem}.metric{grid-column:span 3;min-height:8.3rem}.metric .label{font-size:.75rem;text-transform:uppercase;letter-spacing:.05em;color:var(--muted);font-weight:700}.metric .value{font-size:1.65rem;font-weight:720;letter-spacing:-.02em;margin:.4rem 0 .2rem}.metric .detail{color:var(--muted);font-size:.78rem}.wide{grid-column:span 8}.side{grid-column:span 4}.full{grid-column:1/-1}.card h2{font-size:1rem;margin:0 0 .2rem}.sub{font-size:.78rem;color:var(--muted);margin:0 0 1rem}.chart-wrap{height:18rem;position:relative}.chart{width:100%;height:100%;overflow:visible}.chart .axis{stroke:var(--line);stroke-width:1}.chart .delivered{fill:none;stroke:var(--accent);stroke-width:2.5}.chart .avoided{fill:none;stroke:var(--accent2);stroke-width:2.5}.legend{display:flex;gap:1rem;align-items:center;flex-wrap:wrap;font-size:.78rem;color:var(--muted)}.legend span::before{content:"";display:inline-block;width:.65rem;height:.18rem;border-radius:2px;margin-right:.35rem;vertical-align:middle}.legend .l1::before{background:var(--accent)}.legend .l2::before{background:var(--accent2)}.tool{display:grid;grid-template-columns:minmax(10rem,1fr) 5rem 7rem;gap:.8rem;align-items:center;padding:.65rem 0;border-top:1px solid var(--line);font-size:.82rem}.tool:first-child{border-top:0}.tool .name{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;overflow:hidden;text-overflow:ellipsis}.tool .num{text-align:right;color:var(--muted);font-variant-numeric:tabular-nums}.bar{height:.35rem;background:var(--surface2);border-radius:999px;overflow:hidden;margin-top:.3rem}.bar i{display:block;height:100%;background:var(--accent);border-radius:inherit}.saving-box{display:grid;grid-template-columns:1fr 1fr;gap:.8rem}.saving-box div{padding:.85rem;background:var(--surface2);border-radius:10px}.saving-box b{display:block;font-size:1.15rem}.saving-box span{display:block;font-size:.74rem;color:var(--muted);margin-top:.15rem}.price{margin-top:1rem;padding-top:1rem;border-top:1px solid var(--line)}.price label{font-size:.78rem;font-weight:700;color:var(--muted)}.price-row{display:flex;gap:.5rem;align-items:center;margin-top:.4rem}.price input{min-width:0;width:8rem;padding:.55rem .65rem;border:1px solid var(--line);border-radius:9px;background:var(--surface2);color:var(--text);font:inherit}.money{font-size:1.1rem;font-weight:700;color:var(--good)}.notice{padding:1rem;border:1px solid var(--line);border-radius:12px;background:var(--surface);color:var(--muted);font-size:.86rem}.notice strong{color:var(--text)}.empty{color:var(--muted);font-size:.84rem;padding:.8rem 0}.hidden{display:none!important}.error{border-color:#f1aeb5;background:#fff1f2;color:#9f1239}.foot{margin-top:1rem;color:var(--muted);font-size:.75rem}.foot code{font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace}
@media(prefers-color-scheme:dark){:root{--bg:#0f1115;--surface:#171a20;--surface2:#20242c;--text:#e6e8eb;--muted:#9aa4b2;--line:#2c323b;--accent:#6b9af4;--accent2:#a78bfa;--good:#6ee7b7}.card,.nav a,.range button{box-shadow:none}}
@media(max-width:900px){.metric{grid-column:span 6}.wide,.side{grid-column:1/-1}.hero{align-items:start;flex-direction:column}.chart-wrap{height:15rem}}
@media(max-width:560px){.metric{grid-column:1/-1}.shell{padding-top:1rem}.topbar{align-items:flex-start}.brand{font-size:1.6rem}.tool{grid-template-columns:1fr 4rem}.tool .tokens{display:none}.saving-box{grid-template-columns:1fr}.nav{flex-direction:column;align-items:stretch}}
</style></head><body><div class="shell">
<header class="topbar"><div class="brand">SlncTrZ</div><nav class="nav"><a href="/owner">Owner Console</a><a href="/usage" aria-current="page">Usage</a></nav></header>
<section id="auth-required" class="notice hidden"><strong>Owner sign-in required.</strong> Usage data stays private to the Owner Console session. <a href="/owner">Sign in at /owner</a>, then return here.</section>
<div id="dashboard" class="hidden">
<section class="hero"><div><h1>Usage &amp; context efficiency</h1><p>Observed MCP traffic and estimated context avoided by progressive Agent Skills. Webchat prompts, normal model replies, and provider billing are not measured.</p></div><div class="range" id="range"><button data-range="24h" class="active">24h</button><button data-range="7d">7 days</button><button data-range="30d">30 days</button><button data-range="all">All</button></div></section>
<section id="usage-error" class="notice error hidden"></section>
<div class="grid">
<section class="card metric"><div class="label">Gateway traffic</div><div id="total-tokens" class="value">0</div><div id="calls" class="detail">0 MCP exchanges</div></section>
<section class="card metric"><div class="label">Gateway → client</div><div id="output-tokens" class="value">0</div><div class="detail">Estimated context delivered</div></section>
<section class="card metric"><div class="label">Avoided context</div><div id="avoided-tokens" class="value">0</div><div id="reduction" class="detail">0% reduction</div></section>
<section class="card metric"><div class="label">Estimated cost avoided</div><div id="cost-saved" class="value">$0.00</div><div class="detail">At your selected input-token rate</div></section>
<section class="card wide"><h2>Context over time</h2><p class="sub">Delivered output across the gateway versus context the harness did not eagerly load.</p><div class="chart-wrap"><svg id="chart" class="chart" viewBox="0 0 800 260" role="img" aria-label="Usage over time"></svg></div><div class="legend"><span class="l1">Delivered</span><span class="l2">Avoided</span></div></section>
<section class="card side"><h2>Harness efficiency</h2><p class="sub">Baseline: bootstrap context plus all active SKILL.md bodies loaded eagerly.</p><div class="saving-box"><div><b id="potential">0</b><span>Potential eager</span></div><div><b id="disclosed">0</b><span>Actually disclosed</span></div><div><b id="avoided-small">0</b><span>Avoided</span></div><div><b id="contexts">0</b><span>Contexts measured</span></div></div><div class="price"><label for="price">Input price per 1M tokens (USD)</label><div class="price-row"><input id="price" inputmode="decimal" type="number" min="0" step="0.01" value="0"><span id="price-result" class="money">$0.00</span></div></div></section>
<section class="card full"><h2>Where context goes</h2><p class="sub">Aggregated by MCP method/tool. No prompt, arguments, file content, command output, or credentials are stored.</p><div id="tools"></div></section>
</div>
<p class="foot">Estimator: <code id="estimator">utf8-bytes-v1</code> (roughly 4 UTF-8 bytes per token). Byte counts are measured at the gateway boundary; token and dollar figures are estimates.</p>
</div></div>
<script>
const q=id=>document.getElementById(id);let range='24h',lastSavings=null;
const n=new Intl.NumberFormat(undefined,{maximumFractionDigits:1,notation:'compact'});const full=new Intl.NumberFormat();
function fmt(v){return n.format(Number(v||0))}function money(v){return new Intl.NumberFormat(undefined,{style:'currency',currency:'USD'}).format(Number(v||0))}function savedPrice(){try{return localStorage.getItem('slnctrz_usage_input_price')}catch{return null}}function savePrice(v){try{localStorage.setItem('slnctrz_usage_input_price',v)}catch{}}
async function api(path){const r=await fetch(path,{headers:{accept:'application/json'}});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d?.error?.message||('HTTP '+r.status));return d}
function renderChart(points){const svg=q('chart');svg.innerHTML='';const w=800,h=260,p=24;const vals=points.flatMap(x=>[x.deliveredEstimatedTokens||0,x.avoidedEstimatedTokens||0]);const max=Math.max(1,...vals);for(let i=0;i<4;i++){const y=p+(h-p*2)*i/3;const l=document.createElementNS('http://www.w3.org/2000/svg','line');l.setAttribute('x1',String(p));l.setAttribute('x2',String(w-p));l.setAttribute('y1',String(y));l.setAttribute('y2',String(y));l.setAttribute('class','axis');svg.appendChild(l)}if(!points.length)return;const coords=(key)=>points.map((x,i)=>{const xx=points.length===1?w/2:p+(w-p*2)*i/(points.length-1);const yy=h-p-(Number(x[key]||0)/max)*(h-p*2);return xx+','+yy}).join(' ');for(const [key,cls] of [['deliveredEstimatedTokens','delivered'],['avoidedEstimatedTokens','avoided']]){const poly=document.createElementNS('http://www.w3.org/2000/svg','polyline');poly.setAttribute('points',coords(key));poly.setAttribute('class',cls);poly.setAttribute('vector-effect','non-scaling-stroke');svg.appendChild(poly)}}
function renderTools(rows){const el=q('tools');el.innerHTML='';if(!rows.length){el.innerHTML='<div class="empty">No measured MCP traffic in this range.</div>';return}const max=Math.max(1,...rows.map(x=>x.estimatedTotalTokens||0));for(const row of rows){const div=document.createElement('div');div.className='tool';const main=document.createElement('div');main.innerHTML='<div class="name"></div><div class="bar"><i></i></div>';main.querySelector('.name').textContent=row.toolId;main.querySelector('i').style.width=Math.max(1,(row.estimatedTotalTokens/max)*100)+'%';const calls=document.createElement('div');calls.className='num';calls.textContent=full.format(row.calls)+' calls';const tokens=document.createElement('div');tokens.className='num tokens';tokens.textContent=fmt(row.estimatedTotalTokens)+' tok';div.append(main,calls,tokens);el.appendChild(div)}}
function updateCost(){const price=Math.max(0,Number(q('price').value)||0);savePrice(String(price));const saved=(lastSavings?.avoidedEstimatedTokens||0)/1000000*price;q('cost-saved').textContent=money(saved);q('price-result').textContent=money(saved)}
async function load(){q('usage-error').classList.add('hidden');try{const [s,t,tools,save,health]=await Promise.all([api('/owner/api/usage/summary?range='+encodeURIComponent(range)),api('/owner/api/usage/timeseries?range='+encodeURIComponent(range)),api('/owner/api/usage/tools?range='+encodeURIComponent(range)),api('/owner/api/usage/savings?range='+encodeURIComponent(range)),api('/owner/api/usage/health')]);lastSavings=save;q('total-tokens').textContent=fmt(s.estimatedTotalTokens);q('output-tokens').textContent=fmt(s.estimatedOutputTokens);q('calls').textContent=full.format(s.calls)+' MCP exchanges';q('avoided-tokens').textContent=fmt(save.avoidedEstimatedTokens);q('reduction').textContent=save.reductionPercent.toFixed(1)+'% estimated reduction';q('potential').textContent=fmt(save.potentialEagerEstimatedTokens);q('disclosed').textContent=fmt(save.disclosedEstimatedTokens);q('avoided-small').textContent=fmt(save.avoidedEstimatedTokens);q('contexts').textContent=full.format(save.contexts);q('estimator').textContent=s.estimatorId;renderChart(t);renderTools(tools);updateCost();if(health.degraded){q('usage-error').textContent='Usage telemetry is degraded: '+full.format(health.droppedEvents)+' event(s) dropped; last failure '+(health.lastFailureClass||'unknown')+'.';q('usage-error').classList.remove('hidden')}}catch(e){q('usage-error').textContent='Usage telemetry unavailable: '+(e.message||String(e));q('usage-error').classList.remove('hidden')}}
async function boot(){try{await api('/owner/api/session');q('dashboard').classList.remove('hidden');const saved=savedPrice();if(saved!==null)q('price').value=saved;q('price').addEventListener('input',updateCost);q('range').addEventListener('click',e=>{const b=e.target.closest('button[data-range]');if(!b)return;range=b.dataset.range;q('range').querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));load()});await load()}catch{q('auth-required').classList.remove('hidden')}}
boot();
</script></body></html>`;
}

export function sendUsagePage(res: ServerResponse): void {
  const payload = page();
  res.writeHead(200, {
    "cache-control": "no-store",
    "content-type": "text/html; charset=utf-8",
    "content-length": Buffer.byteLength(payload),
    "content-security-policy":
      "default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; connect-src 'self'; font-src 'self'; base-uri 'none'; frame-ancestors 'none'",
    "referrer-policy": "no-referrer",
    "x-content-type-options": "nosniff"
  });
  res.end(payload);
}
